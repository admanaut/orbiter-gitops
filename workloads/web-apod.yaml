apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-apod
  name: web-apod-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web-apod
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: web-apod
  name: web-apod-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-apod
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: web-apod
    spec:
      containers:
      - env:
        - name: PORT
          value: "80"
        image: eu.gcr.io/orbiter-279306/orgiter-web-apod:14a142b8cc3a03344ac7bcbf75878f06c0abd9a9-orgiter-web-apod-run2-62
        name: app
        ports:
        - containerPort: 80
